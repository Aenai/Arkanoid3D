
#ifndef Paddle_H
#define Paddle_H

#include <Ogre.h>
#include <OIS/OIS.h>

#include "GameState.h"
#include "CollisionableObject.h"
#include "RecordManager.h"

#define PLAYER 1 << 0  // Mascara para el escenario
#define CUBE1 1 << 1  // Mascara para objetos de tipo 1
#define CUBE2 1 << 2  // Mascara para objetos de tipo 2

class Paddle : public CollisionableObject {
	
 public:
  Paddle (Ogre::SceneNode* newPaddle);

 protected:
  Ogre::SceneNode* _paddle;   	
 	
  //Speed
  float _SpeedPad;

  //Ball Limits
  float _pXMinLimit;
  float _pXMaxLimit;
  
};

#endif
